export interface TopThreeTransaction {name:string,sum:number}
export interface TransactionData {id: number, transaction_name: string, type: string, category: string, amount: number,date:string,user_id:number}
